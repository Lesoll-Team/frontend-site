/** @type {import('next').NextConfig} */

const nextConfig = {
  reactStrictMode: true,
  // images: {
  //   domains: ['res.lesoll-demo.site','example.com', 'otherdomain.com'], // Add the domain(s) of your image(s) here
  // },
};

module.exports = nextConfig;
